/**
 * SectionDivider — uppercase label with a trailing horizontal rule.
 *
 * @param {React.ReactNode} children  section title text
 */
export default function SectionDivider({ children }) {
  return <div className="section-divider">{children}</div>
}
