[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — all routes resolve to index.html so React Router can handle them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
